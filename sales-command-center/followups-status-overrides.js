(() => {
  // September 11+: live daily-plan.js and followups-enhancements.js own the
  // current task list, pipeline snapshot, follow-up counts, and messaging.
  // This file is intentionally kept as a compatibility shim so older cached
  // versions of index.html can still load it without applying stale overrides.
})();
